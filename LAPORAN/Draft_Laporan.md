# Computer Vision Final Exam Report 2025–2026

**Name:** Rifky Nurfaishal  
**ID:** 001202405012

## Custom Object Detection — YOLO11n

---

## 1. Dataset

I collected 92 photos of 2 objects commonly found around my desk: a **PS Controller** and a **Correction Tape**. There are 46 photos for each class. The images were taken across 6 different scenes/locations to provide a good variety of backgrounds, lighting environments, and angles.

| Detail          | Value                           |
| --------------- | ------------------------------- |
| Total           | 92 images                       |
| Class 0         | ps_controller_myunit            |
| Class 1         | correction_tape_myunit          |
| Distribution    | 46 / 46 (balanced)              |
| Split           | 80% train, 20% val (stratified) |
| Annotation Tool | Roboflow                        |

Split distribution:

| Class           | Train | Val |
| --------------- | ----- | --- |
| ps_controller   | 36    | 10  |
| correction_tape | 36    | 10  |

---

## 2. Annotation

Annotations were done using Roboflow. I initially used the "Smart Tools / Find Object" feature to create polygon bounding boxes, which were later exported and automatically converted to the YOLO bounding box format via script.

Example bounding boxes can be viewed in the [`../TRAINING_RESULTS/sample_bbox_quality/`](../TRAINING_RESULTS/sample_bbox_quality/) folder.

**Important Note:** I discovered that the Smart Tools in Roboflow often generated bounding boxes that were excessively large for the PS controller. This became a key finding in my error analysis (see Section 5).

---

## 3. Training

| Parameter  | Value                |
| ---------- | -------------------- |
| Model      | YOLO11n (yolo11n.pt) |
| Image size | 640                  |
| Epochs     | 50                   |
| Batch size | 16                   |
| Hardware   | CPU (i5-12400F)      |
| Time taken | ~8 minutes           |

---

## 4. Results

### Overall Metrics

| Metric       | Value |
| ------------ | ----- |
| mAP@0.5      | 0.817 |
| mAP@0.5:0.95 | 0.750 |
| Precision    | 0.989 |
| Recall       | 0.750 |

### Per-Class Metrics

| Class           | Precision | Recall | mAP@0.5 | mAP@0.5:0.95 |
| --------------- | --------- | ------ | ------- | ------------ |
| ps_controller   | 0.994     | 1.000  | 0.995   | 0.995        |
| correction_tape | 1.000     | 0.514  | 0.645   | 0.545        |

The PS controller performed nearly perfectly on the validation set, but the correction tape had a low recall of 0.514 (meaning almost half were missed). Interestingly, when I tested the model live with my webcam, the results were completely reversed—the PS controller struggled to be detected in real life. A detailed explanation of this issue is provided in Section 5.

---

## 5. Failure Analysis

### Case 1: PS Controller Confused with Shirt (Color/Texture Bias)

In my initial testing, I found that the model frequently misidentified my shirt as the PS controller. I initially hypothesized this was due to overly large bounding boxes generated by Roboflow's Smart Tools capturing too much background.

To fix this, **I manually re-annotated the entire dataset**, ensuring tight, precise bounding boxes that only contained the PS controller itself. However, after retraining the model with these corrected, tight bounding boxes, **the issue persisted**. The model still occasionally detects my shirt as the controller during live webcam tests.

**Analysis of the Root Cause:**
Since the bounding boxes are now correct, the error stems from how the model learns features from a small, constrained dataset.

1. **Color & Texture Overlap:** The PS controller is a matte, dark-colored object without many distinct, high-contrast internal patterns. If the shirt worn during testing shares a similar dark color and smooth texture, the model's convolutional filters may activate highly on both.
2. **Lack of Negative Examples:** The training dataset (92 images) likely features the controller against specific backgrounds (desk, floor). The model hasn't seen enough examples of _shirts_ in the background without a controller present to learn that a shirt is _not_ a controller. It hasn't learned the distinct boundaries and specific button layouts robustly enough to separate them from a similarly colored blob.
3. **Shape vs. Color Bias:** YOLO models, especially lightweight versions like YOLO11n trained on small datasets, can sometimes over-rely on low-level features (like color and dark regions) rather than high-level geometric understanding (the specific shape of the grips and D-pad).

**Conclusion:** Precise bounding boxes are necessary but not always sufficient when objects share visual characteristics (color/texture) with background elements not strongly represented as negative space in the training data.

### Case 2: Low Recall for the Correction Tape

The recall for the correction tape was lower than the PS controller. This was likely due to:

- The object being relatively small within the frame.
- A dataset of 92 images being too small to provide sufficient scaling and rotational variation.
- Lighting conditions making it difficult to distinguish the highly reflective, mostly clear/white tape dispenser from light-colored backgrounds or glaring light sources.

### Case 3: Validation vs. Webcam Performance Gap

On the validation set, the PS controller achieved near-perfect mAP. However, it still struggled in the live webcam test (specifically regarding the shirt issue). This happens because the validation images, despite being a separate split, still originated from the same collection sessions as the training data. They share the same distribution of lighting, angles, and backgrounds.

When tested via webcam in a slightly different posture or lighting condition, the model's confidence drops or misfires on similar textures, showing that 92 images, even properly annotated, lack the generalization power needed for robust real-world deployment.

### Summary of Failure Modes

| Issue                           | Impact                                                                      |
| ------------------------------- | --------------------------------------------------------------------------- |
| Texture & Color Ambiguity       | The model confuses dark, matte clothing with the dark, matte controller.    |
| Insufficient Negative Examples  | The model doesn't know what a "shirt" looks like to explicitly ignore it.   |
| Small dataset size (92 images)  | The model is highly prone to overfitting locally and failing to generalize. |
| Lack of environmental variation | Noticeable performance drop in new conditions (webcam test).                |

**Conclusion:** Even with perfect annotations, a small dataset limits the model's ability to differentiate the target object from visually similar "distractor" objects in the real world. Validation metrics can be highly misleading if the validation set perfectly mirrors the training conditions.

**Possible Improvements:**

1. Perform manual annotation, ensuring tight bounding boxes around only the core object.
2. Expand the dataset with more images across diverse conditions and zoom ranges.
3. Visually verify bounding boxes prior to starting any training loop.

---

## 6. Submission Files Tree

```text
UAS_Computer_Vision/
├── README.md
├── LAPORAN/Draft_Laporan.md
├── DATASET_YOLO/
│   ├── data.yaml
│   ├── images/ (train + val)
│   └── labels/ (train + val)
├── TRAINING_RESULTS/
│   ├── metrics.txt
│   ├── results.png, confusion_matrix.png
│   ├── PR_curve.png, F1_curve.png
│   ├── val_batch0_pred.jpg
│   └── sample_bbox_quality/
├── MODEL/ (best.pt, last.pt)
├── DEMO_VIDEO/
│   ├── pred_images/
│   ├── Demo Video.mp4
│   └── demo_deteksi.mp4
└── CODE/
    ├── prepare_dataset.py
    ├── train_and_eval.py
    ├── infer_demo.py
    └── requirements.txt
```
